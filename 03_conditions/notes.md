CONDITIONS IN PYTHON

Conditions allow the program to make decisions.

Python can check if something is True or False and execute different code based on the result.

---

IF STATEMENT

The `if` statement is used when we want Python to do something only if a condition is True.

Example:

```python
age = 18

if age >= 18:
    print("You are an adult")